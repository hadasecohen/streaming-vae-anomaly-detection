__pycache__/
*.pyc
.ipynb_checkpoints/

git_personal_token
CLAUDE.md
runs/
scratchpad/
# Full-scale real ERA5 source (~550MB, not included in this repo — see
# DATA_LICENSE.md). Local-only; only data/era5/full_scale/split/ (each file
# under GitHub's 100MB limit) is shipped — raw_files/ never is.
data/era5/full_scale/raw_files/

*.egg-info/
.venv/
venv/

# Everything below this point is tracked in this (dev) repo but excluded
# from the production repo by scripts/local/release_to_production.py's
# exclude-list — not by .gitignore, since it needs to stay tracked here.
#   scripts/compare_to_thesis.py, scripts/generate_mini_era5.py,
#   scripts/slice_real_era5.py, scripts/local/, scripts/compile_test_colab.ipynb,
#   notebooks/colab/, git-cmds.txt, standalone_tests/finetuned/,
#   src/env/streaming_env_optuna.py (deleted),
#   src/VAE/lstm_attn_VAE.py (deleted), src/VAE/mlp_attn_VAE.py (deleted)

