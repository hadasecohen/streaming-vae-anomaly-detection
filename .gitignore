__pycache__/
*.pyc
.ipynb_checkpoints/

runs/
scratchpad/

# Full-scale real ERA5 source (~550MB, not included in this repo — see
# DATA_LICENSE.md). Local-only; only data/era5/full_scale/split/ (each file
# under GitHub's 100MB limit) is shipped — raw_files/ never is.
data/era5/full_scale/raw_files/

# Smaller 50:50 (12y/12y, ~28% of full-scale) mini-dataset variant, kept
# alongside the current default data/era5/mini_50pct/ for case00's
# three-way dataset-scale comparison — local-only, not shipped (see
# DATA_LICENSE.md).
data/era5/mini_28pct/

*.egg-info/
.venv/
venv/

# Local-only tooling that reaches outside this repo (e.g. into thesis_code,
# a machine-local path not shipped here). Only these scripts' *outputs*
# (small, self-contained reference files) are meant to be committed.
scripts/local/

scripts/compare_to_thesis.py
scripts/generate_mini_era5.py
scripts/slice_real_era5.py
git-cmds.txt
